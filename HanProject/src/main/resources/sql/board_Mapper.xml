<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

    <select id="selectList" resultType="spring.command.BoardListCommand" >
        select b_number,b_subject,b_writer,b_regdate,
        (select count(*) from comments c where c.b_number=b.b_number) c_count 
        from board b order by b_number desc
    </select>
    
    <select id="countComment"></select>
    
    <select id="search_subject" parameterType="string" resultType="spring.command.BoardCommand">
        select * from board where b_subject like '%'||#{string}||'%'
    </select>
    
    <select id="search_writer" parameterType="string" resultType="spring.command.BoardCommand">
        select * from board where b_writer like '%'||#{string}||'%'
    </select>
    
    <select id="selectOne" parameterType="int" resultType="spring.command.BoardCommand">
		select * from board where b_number=#{int}        
    </select>
    
    
    
    
</mapper>